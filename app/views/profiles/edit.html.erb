<h1>Add Profile Picture</h1>

<%= simple_form_for(@profile) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :photo, as: :file %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Upload", :type => "submit" %>
  </div>
<% end %>

<%= link_to 'Cancel', projects_path %>
